#include <iostream>
#include <player/player.h>

void Player::talk() {
    isTalking = false;
    if (isTalking = true) {
        printf("Talking ...");
    } else {
        printf("Not talking");
    }
}